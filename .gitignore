# Local Claude/agent state
.claude/

# Validation screenshots (kept locally, not part of the product)
*.png

# Node deps
node_modules/
.env
.env.local

# Claude/superpowers internal planning docs.
# - specs/ ARE tracked (canonical design documents, useful for colleagues)
# - plans/ are NOT tracked (work-in-progress, regenerated by writing-plans skill)
docs/superpowers/plans/

# NOTE: dist/ IS committed — it's how users install via raw.githubusercontent.com

# OS noise
Thumbs.db
.DS_Store
